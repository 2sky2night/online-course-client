import { createStyles } from "antd-style";

export const useLayoutStyles = createStyles(() => {
  return {
    layout: {
      height: "100svh",
      display: "flex",
      flexDirection: "column",
    },
  };
});
